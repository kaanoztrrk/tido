import 'package:flutter/material.dart';

class PomodoroHistory extends StatelessWidget {
  const PomodoroHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
